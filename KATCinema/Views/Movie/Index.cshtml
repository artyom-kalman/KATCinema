@model IEnumerable<Movie>

<div class="album py-5">
    <div class="container">

        <div class="row">
        
            @foreach(Movie item in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4 box-shadow bg-dark ">
                        <img class="card-img-top poster" src="@item.PosterUrl" alt="Постер для @item.Title">
                        <div class="card-body">
                            <p class="card-text text-white">@item.Title</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                            <a class="btn btn-sm btn-outline-primary" asp-controller="Movie" asp-action="Detail" asp-route-id="@item.Id" >Сеансы</a>
                                </div>
                                <small class="text-muted">@item.Duration минут</small>
                            </div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
